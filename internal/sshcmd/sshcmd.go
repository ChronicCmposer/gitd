// Package sshcmd is the SSH_ORIGINAL_COMMAND gateway: a hand-written
// single-quote-aware tokenizer (Phase 3.1), repo allowlist validation, and
// argv exec of git-upload-pack / git-receive-pack. Logic lands in Phase 3.
package sshcmd

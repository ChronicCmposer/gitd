# Dist pipeline build outputs (deterministic artifacts). The content-addressed
# rootfs cache lives OUTSIDE the repo (see chroot.sh): ${XDG_CACHE_HOME:-$HOME/.cache}/gitd-dist
# Never commit these: the pins in //:dist_pins.bzl are the committed state
# (generated by gen-dist-pins.sh from determinism-checked tarballs).
/out/
/.work-*/
/.check-*/
/.stage-*/